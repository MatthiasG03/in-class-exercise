# --- PDFs (meist Build-Ergebnis; aus TeX neu erzeugen) ---
*.pdf

# --- macOS ---
.DS_Store

# --- LaTeX / Beamer Hilfsdateien (nicht versionieren) ---
*.aux
*.bbl
*.bcf
*.blg
*.fdb_latexmk
*.fls
*.log
*.nav
*.out
*.run.xml
*.snm
*.synctex.gz
*.toc
*.vrb

# Synctex temporär / Lock (nie committen)
*synctex(busy)*
